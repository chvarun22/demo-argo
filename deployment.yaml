apiVersion: v1
kind: Namespace
metadata: 
  name: demo
---
kind: Service
apiVersion: v1
metadata:
  name: demo-webapp:0.1.366
  namespace: demo
spec:
  selector:
    app:0.1.366
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
---
apiVersion: app:0.1.366
kind: Deployment
metadata:
  name: demo-webapp:0.1.366
  namespace: demo
  labels:
    app:0.1.366
spec:
  replicas: 1
  selector:
    matchLabels:
      app:0.1.366
  template:
    metadata:
      labels:
        app:0.1.366
    spec:
      containers:
      - name: demo-webapp:0.1.366
        image: gcr.io/sublime-state-352111/demo:$TAG
        ports:
        - containerPort: 80
