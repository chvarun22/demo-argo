apiVersion: v1
kind: Namespace
metadata: 
  name: demo
---
kind: Service
apiVersion: v1
metadata:
  name: demo-webapp:0.1.374
  namespace: demo
spec:
  selector:
    app:0.1.374
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
---
apiVersion: app:0.1.374
kind: Deployment
metadata:
  name: demo-webapp:0.1.374
  namespace: demo
  labels:
    app:0.1.374
spec:
  replicas: 1
  selector:
    matchLabels:
      app:0.1.374
  template:
    metadata:
      labels:
        app:0.1.374
    spec:
      containers:
      - name: demo-webapp:0.1.374
        image: gcr.io/$GOOGLE_PROJECT_ID/demo:$TAG
        ports:
        - containerPort: 80
